% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/jxl.R
\name{read_jxl_as}
\alias{read_jxl_as}
\title{Read JXL with automatic format conversion}
\usage{
read_jxl_as(source, format = c("chw", "hwc", "auto"), numeric = TRUE)
}
\arguments{
\item{source}{A raw vector or path to a JXL file}

\item{format}{Output format: "chw" (channels first), "hwc" (channels last), or "auto" (same as input)}

\item{numeric}{Logical; convert to 0-1 real numbers}
}
\value{
Image array in requested format
}
\description{
Convenience wrapper that reads JXL and converts to specified format.
}
\examples{
\donttest{
img <- array(runif(5 * 5 * 3), dim = c(5, 5, 3))
jxl_data <- write_jxl(img)

# Read as channels-first (default JXL format)
img_chw <- read_jxl_as(jxl_data, "chw")
dim(img_chw) # c(4, 5, 5) - note RGBA output

# Read as channels-last (common R format)
img_hwc <- read_jxl_as(jxl_data, "hwc")
dim(img_hwc) # c(5, 5, 4)
}
}
