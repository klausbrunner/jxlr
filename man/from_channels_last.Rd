% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/jxl.R
\name{from_channels_last}
\alias{from_channels_last}
\alias{to_channels_last}
\title{Convert image arrays between different dimension conventions}
\usage{
from_channels_last(img)

to_channels_last(img)
}
\arguments{
\item{img}{Image array}
}
\value{
Converted image array
}
\description{
Helper functions to convert between R's typical array formats and JXL's expected format.
}
\examples{
\donttest{
# Convert from channels-last (common in R) to channels-first (JXL)
img_hwc <- array(runif(10 * 8 * 3), dim = c(10, 8, 3)) # Height x Width x Channels
img_chw <- from_channels_last(img_hwc) # Channels x Height x Width
dim(img_chw) # c(3, 10, 8)

# Convert back
img_back <- to_channels_last(img_chw)
dim(img_back) # c(10, 8, 3)
}
}
