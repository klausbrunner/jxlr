% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/jxl.R
\name{write_jxl_anim}
\alias{write_jxl_anim}
\title{Write JPEG XL animations}
\usage{
write_jxl_anim(
  frames,
  target = NULL,
  durations = 100,
  quality = 90,
  effort = 7,
  loop_count = 0
)
}
\arguments{
\item{frames}{List of 3D arrays (width x height x channels) with values in \[0,1\]}

\item{target}{Output file path, or NULL to return raw vector}

\item{durations}{Frame duration(s) in milliseconds. Single value applies to all frames, vector must match frame count}

\item{quality}{Compression quality: 0-100 for lossy, NA for lossless}

\item{effort}{Compression effort: 1-9 (1=fastest, 9=best compression, default=7)}

\item{loop_count}{Number of loops: 0 for infinite, NA for single play}
}
\value{
If target is NULL, returns raw vector with class "jxl_anim".
Otherwise, invisibly returns the file path.
}
\description{
Write frame lists to JPEG XL animation format with timing and loop control.
}
\examples{
\donttest{
frames <- list(
  array(runif(5 * 5 * 3), dim = c(5, 5, 3)),
  array(runif(5 * 5 * 3), dim = c(5, 5, 3)),
  array(runif(5 * 5 * 3), dim = c(5, 5, 3))
)

# Different duration per frame
anim1 <- write_jxl_anim(frames, durations = c(200, 300, 250))

# Same duration for all frames
anim2 <- write_jxl_anim(frames, durations = 200)
cat("Animation sizes:", length(anim1), length(anim2), "bytes\n")
}
}
